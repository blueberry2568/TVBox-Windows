<UserControl
    x:Class="FongMi.TV.UI.Controls.PosterCard"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FongMi.TV.UI.Controls"
    IsTabStop="True"
    UseSystemFocusVisuals="True">

    <Grid x:Name="Root"
          Width="150"
          Background="Transparent"
          PointerEntered="OnPointerEntered"
          PointerExited="OnPointerExited"
          Tapped="OnTapped">
        <Grid x:Name="Card"
              Height="225"
              CornerRadius="8"
              Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
              BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
              BorderThickness="1"
              SizeChanged="OnCardSizeChanged">
            <local:PosterImage x:Name="Poster"/>
            <!-- 底部渐变遮罩 + 标题 -->
            <Border VerticalAlignment="Bottom" Padding="8,24,8,6">
                <Border.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                        <GradientStop Color="#00000000" Offset="0"/>
                        <GradientStop Color="#B3000000" Offset="1"/>
                    </LinearGradientBrush>
                </Border.Background>
                <TextBlock x:Name="TitleText"
                           Foreground="White"
                           FontSize="13"
                           TextWrapping="NoWrap"
                           TextTrimming="CharacterEllipsis"/>
            </Border>
            <!-- 右上角备注徽标 -->
            <Border x:Name="RemarkBadge"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Top"
                    Margin="0,6,6,0"
                    Padding="6,2"
                    CornerRadius="4"
                    Visibility="Collapsed"
                    Background="{ThemeResource AccentFillColorDefaultBrush}">
                <TextBlock x:Name="RemarkText"
                           FontSize="11"
                           Foreground="{ThemeResource TextOnAccentFillColorPrimaryBrush}"/>
            </Border>
        </Grid>
    </Grid>
</UserControl>
